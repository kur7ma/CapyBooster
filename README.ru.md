<p align="center">
  <img src="assets/logo.png" width="620"
       alt="CapyBooster: твикер Windows, который меряет, что изменил">
</p>

<p align="center">
  <b>Твикер Windows, который меряет, что изменил, и говорит вслух,<br>
  когда замер ничего не показал.</b>
</p>

<p align="center">
  <a href="README.md">English</a> ·
  <a href="FAQ.ru.md">FAQ</a> ·
  <a href="ИЗМЕНЕНИЯ.md">Что менялось</a> ·
  <a href="https://github.com/kur7ma/CapyBooster/releases/latest/download/CapyBooster-latest.zip"><b>Скачать</b></a>
</p>

Паки оптимизации обычно везут список правок реестра и обещание. CapyBooster
делает короткий замер после каждого модуля, сравнивает с предыдущим и печатает
вердикт даже тогда, когда вердикт звучит как «разница меньше нашего
собственного шума».

Каждое значение сохраняется в резервную копию до правки, и любой модуль
откатывается одной командой. В каждой из 118 карточек твиков написано, чем за
него платят, а не только что он даёт.

---

## Быстрый старт

Скачайте [архив](https://github.com/kur7ma/CapyBooster/releases/latest/download/CapyBooster-latest.zip),
распакуйте **целиком** в любую папку и запустите **START.cmd**.

```
-- CapyBooster — мастер оптимизации Windows
   CapyBooster v4.4.8   сборка 2026-08-26   коммит c07dd18

  Язык / Language

    [Enter]  русский  (определён по системе)
    [e]      English

    >
```

Мастер спрашивает язык, запоминает ответ и дальше ведёт весь прогон:
предполётная проверка, точка восстановления, остатки прежнего пака,
инструменты, базовый замер, группы твиков с замером после каждой и HTML-отчёт.

```
==> Предполётная проверка:
[+]   кодировка: все скрипты в порядке
[+]   PresentMon: установлен (PresentMon-2.5.1-x64.exe)
[+]   защита системы: включена (точка восстановления будет создана)
[+]   перезагрузка не отложена
[+]   Windows не под внешним управлением

  -- Подготовка --
   ok  Точка восстановления и снимок системы
   ok  Остатки прежнего пака: проверка и решение
   ok  Инструменты: PresentMon и профиль NVIDIA
   ok  Базовый быстрый замер (без игры)
  -- ОС --
   ok  План электропитания
   ok  Службы
   ok  Телеметрия
   ok  Задачи планировщика
   ok  Накопители
   ok  Драйверы
   ok  Твики Windows 11
   ok  Сетевой адаптер
       VBS / HVCI: отключение защиты на основе виртуализации  (нужна перезагрузка)
  -- CPU --
   ok  Раскладка прерываний  (нужна перезагрузка)
   ok  Быстрый замер после перезагрузки
  -- GPU --
   ok  Профиль NVIDIA для CS2
  -- Игра --
   ok  MMCSS: приоритеты мультимедиа
       Кванты планировщика и приоритет игры
       Фоновая запись видео
       Гигиена памяти: задача очистки перед игрой
  -- Итог --
       HTML-отчёт
       Архив диагностики для отправки
```

На любом шаге можно нажать `a` и доделать остальное без вопросов. Перезагрузка
подтверждается и в этом режиме.

---

## Чем отличается

### Замер может ничего не показать, и вам об этом скажут

После каждого модуля пак меряет опоздание потока и сравнивает с предыдущим
прогоном:

```
МЕТКА             P99, мкс  P99.8, мкс  РАЗБРОС      ПРОТИВ ПРЕДЫДУЩЕГО
baseline            93,46      216,3    ±21,9 P99    первый
after-power          21,9     146,31    ±29,8 P99    опоздания уменьшились
                                                     P99 -71,6 мкс, шум ±29,81
after-nvprofile     27,41     150,98      ±21 P99    неразличимо на фоне
                                                     разброса самого замера
after-win11           5,7       25,1     ±0,6 P99    сравнивать нельзя
                                                     SMT выключался между ними
```

Разница меньше разброса между повторами получает вердикт «неразличимо». Если
между замерами менялась топология процессора, пак помечает их как несравнимые,
а не вычитает один из другого.

На очень тихой машине, где измеритель не различает ничего, пак пропускает
синтетические замеры и объясняет причину — вместо столбца нулей и одиннадцати
вердиктов «неразличимо».

### В каждой карточке написана цена

В карточке: на что твик влияет, насколько это подтверждено (`подтверждено`,
`ожидаемо`, `спорно`), какого эффекта ждать (`средний FPS`, `просадки`,
`задержка ввода`, `не про FPS`) и размен. Из модуля VBS/HVCI:

> Отключает безопасность на основе виртуализации и целостность памяти, включая
> запуск гипервизора в BCD. Это размен защиты и виртуализации на
> производительность, а не бесплатная оптимизация.

### Популярные твики получают отказ с причиной

В отчёте перечислены 22 приёма, которых пак не делает. Часть из них:

| Отвергнуто | Почему |
|---|---|
| Отключение UAC | На Windows 11 ломает все приложения из Store, Параметры, Терминал и поиск в Пуске |
| Отключение HPET через BCD | `disabledynamictick` на Zen 5 ухудшает буст |
| Резидентная очистка памяти (ISLC) | Список ожидания — кэш файлов, а не мусор |
| Глобальный таймер 1 мс | Разрешение таймера с Windows 10 2004 попроцессное |
| Отключение SysMain на NVMe | Развенчано: выигрыша нет, предзагрузка перестаёт работать |
| Удаление служб через `sc delete` | Необратимо без починки хранилища компонентов |
| `dism /ResetBase` | Освобождает место и делает откат обновлений невозможным |
| Отключение теневого копирования | На нём стоит точка восстановления, которую создаёт пак |
| Nagle, TcpAckFrequency, TCPNoDelay | Игровой трафик идёт по UDP, где алгоритм Нейгла не работает |
| Удаление системных пакетов AppX | Приложение, которое вы не открываете, процессор не занимает |
| Снятие оптимизаций полноэкранного режима | На Windows 11 отнимает Auto HDR и переменную частоту обновления |
| Включение игрового режима и HAGS | На одних связках драйвера и игры помогает, на других вредит |

### Ваши игры — найденные и названные

В папке `games` лежат профили Counter-Strike 2, Valorant, Apex Legends и Delta
Force. Пак находит установленное, читает настоящее имя исполняемого файла с
диска и пишет ключ высокого приоритета только под это имя. Ключ, написанный под
угаданное имя, выглядел бы применённым и не делал бы ничего.

В профилях записаны и факты про античиты, и пак ими пользуется. Режиму загрузки
Vanguard по требованию нужна целостность памяти, поэтому шаг VBS/HVCI
предупредит до отключения, если на машине стоит Valorant, а проверка системы
покажет конфликт, если отключение уже произошло.

### Без резервной копии не пишется ничего

Каждый модуль сохраняет копию до того, как что-то изменит, — и в папку пака, и в
общесистемное хранилище, чтобы удаление папки не забрало с собой путь отката. У
каждого модуля свой `revert.ps1`, а прогон начинается с точки восстановления
системы.

### Он проверяет свою работу после

```
-- Проверка состояния системы

  Раскладка по ядрам
    ядро 0  (LP 0,1  ) - общее
    ядро 3  (LP 6,7  ) - общее + прерывания USB
    ядро 7  (LP 14,15) - общее + прерывания сети

  01 Изоляция ядер
    [--]   ReservedCpuSets       не проводится: все ядра одного класса
           -> системе пришлось бы отдать настоящие ядра; фон вместо этого
              разводит модуль 02, а все ядра остаются у игры

  Итого — OK: 32   OFF: 7   WARN: 4
```

`verify.ps1` читает настоящий реестр, а не журнал самого пака, поэтому ловит и
правки руками, и вторую копию пака, и обновление драйвера, которое стёрло
настройку.

---

## Что он меняет

| Модуль | Что это | Область | Риск |
|---|---|---|---|
| `00-baseline` | Точка восстановления, экспорт реестра, поиск остатков прежнего пака | Основа | низкий |
| `01-cpu-isolation` | Изоляция ядер, **выключена по умолчанию**, см. [FAQ](FAQ.ru.md#почему-изоляция-ядер-выключена-по-умолчанию) | CPU | средний |
| `02-interrupts` | Раскладка прерываний по ядрам | CPU | средний |
| `03-gpu-affinity-bench` | Подбор ядра для прерываний видеокарты, замером | CPU | средний |
| `04-power-plan` | План электропитания | CPU | низкий |
| `05-services` | Службы, только тип запуска, безопасный список | ОС | средний |
| `06-telemetry` | Телеметрия и рекламные подсказки | ОС | низкий |
| `07-scheduled-tasks` | Задачи планировщика | ОС | низкий |
| `08-win11-tweaks` | Твики Windows 11, снятие акселерации мыши | ОС | низкий |
| `09-network-nic` | Сетевой адаптер, очереди RSS | Сеть | средний |
| `10-storage` | TRIM и автообслуживание | Диски | низкий |
| `11-vbs-hvci` | VBS / HVCI | ОС | **высокий** |
| `12-drivers` | Автоустановка драйверов | Драйверы | низкий |
| `13-nvidia-profile` | NVIDIA Profile Inspector, профиль CS2 | GPU | низкий |
| `14-mmcss` | Приоритеты MMCSS для задачи Games | CPU | низкий |
| `15-cs2` | Настройки CS2 | Игра | низкий |
| `16-cs2-bench` | Автозамер в CS2 | Игра | низкий |
| `17-memory-hygiene` | Одноразовая очистка списка ожидания, по требованию | Память | низкий |
| `18-autobench` | Автозамер отзывчивости | Основа | низкий |
| `19-priority-separation` | Кванты планировщика, высокий приоритет вашим играм | CPU | низкий |
| `20-recording` | Фоновая запись Xbox Game Bar | GPU | низкий |
| `21-amd-profile` | Разбор настроек Radeon для соревновательной игры | GPU | низкий |
| `95-report` | HTML-отчёт | Основа | низкий |
| `exp-timer` | Эксперимент: нужен ли TimerTool на Windows 11 | Опыт | низкий |

---

## Что нужно

* Windows 11, сборка 26100 (24H2) или новее. На Windows 10 работает, но
  затачивался не под неё.
* Права администратора.
* Windows PowerShell 5.1, тот, что уже есть в Windows. Ставить нечего.
* Ни сторонних модулей, ни `wmic`, ни VBScript. Ворота качества не дадут собрать
  релиз, если что-то из этого вернётся, см.
  [FAQ](FAQ.ru.md#что-за-ворота-качества).

---

## Язык

Мастер спрашивает при первом запуске, предлагая язык, определённый по языку
интерфейса Windows, и запоминает ответ. Сменить потом:

```
START.cmd -Lang en
```

Поддерживаются `ru` и `en`. Переведены мастер, проверка состояния системы,
HTML-отчёт и все 118 карточек твиков. Подробный вывод внутри отдельных модулей
остался русским: он скрыт по умолчанию и показывается по просьбе.

---

## Обновление

Мастер проверяет обновления при запуске и предлагает поставить, показав сначала
список изменений. Замеры, резервные копии и прогресс прогона остаются на месте.

```
powershell -NoProfile -ExecutionPolicy Bypass -File ".\tools\Update-Pack.ps1"
```

Только проверить, не устанавливая:

```
powershell -NoProfile -ExecutionPolicy Bypass -File ".\tools\Update-Pack.ps1" -Check
```

Обновление правит файлы **на месте**. Если вместо этого распаковать новый архив
в *новую* папку, задачи планировщика от старой копии продолжат указывать на
старый путь. Пак это обнаружит и скажет, см.
[FAQ](FAQ.ru.md#распаковал-новую-версию-в-новую-папку-что-теперь).

---

## Этот репозиторий

| Файл | Что это |
|---|---|
| [Релизы](https://github.com/kur7ma/CapyBooster/releases/latest) | пак одним архивом плюс `version.json` |
| [ИЗМЕНЕНИЯ.md](ИЗМЕНЕНИЯ.md) · [CHANGELOG.md](CHANGELOG.md) | что менялось, по-русски и по-английски |
| [FAQ.ru.md](FAQ.ru.md) · [FAQ.md](FAQ.md) | вопросы, которые правда возникали |
| [CONTRIBUTING.md](CONTRIBUTING.md) | что проект принимает, а что нет |
| [SECURITY.md](SECURITY.md) | что пак делает с системой и что уходит в сеть |

**Присылайте свои замеры** в
[Обсуждения → Результаты](https://github.com/kur7ma/CapyBooster/discussions/categories/results).
Из отчётов с чужих машин вырос почти весь журнал изменений, включая случаи,
когда пак менял собственные умолчания.

Репозиторий обязан читаться без авторизации: обновлялка ходит анонимно и честно
скажет, если проект закрыт, вместо попытки разобрать страницу входа.

---

## Как выпускается версия

1. поднять `Version` в `version.json`;
2. дописать раздел в `ИЗМЕНЕНИЯ.md` и `CHANGELOG.md` **сверху**: программа
   показывает верхний раздел, поэтому новое всегда идёт первым;
3. приложить пересобранный `win-optimization-latest.zip` к релизу;
4. закоммитить и запушить в `main`.

Формат журнала важен: заголовок раздела — строка `## версия — дата`, каждое
изменение начинается с `**Заголовка.**` жирным. Программа вытаскивает эти
заголовки и показывает их до установки, поэтому первая фраза должна быть понятна
сама по себе.

---

## Как это сопровождается

В журнале изменений записаны баги, которые пак выпускал, включая те, что делали
хуже. Два из них:

**Изоляция ядер была включена по умолчанию и стоила кадров.** Резервирование
ядер только *убирает* их из набора, который Windows раздаёт по умолчанию;
посадить туда игру оно не может. На одной машине PUBG упал примерно с 400 FPS
до 300. Шаг теперь выключен по умолчанию, а уже применённая изоляция
показывается как находка с командой отката.

**Замер однажды объявил регрессом 0,3 микросекунды.** На очень тихой машине оба
прогона дали одинаковые числа, разброс вышел ровно нулевым, а любой запас,
умноженный на ноль, тоже ноль. Теперь у шума есть пол в одну микросекунду, а
прогонов три вместо двух.

Если что-то здесь сделало вашей машине хуже, присылайте. В `START.cmd` есть шаг,
который собирает архив диагностики с замаскированным именем учётной записи.
